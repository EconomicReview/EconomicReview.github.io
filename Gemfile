source "https://rubygems.org"

# GitHub Pages' exact production stack (Jekyll 3.10.x + whitelisted plugins).
# Used for local preview only; GitHub builds the site with its own copy.
gem "github-pages", group: :jekyll_plugins

# `jekyll serve` needs webrick explicitly on Ruby >= 3.
gem "webrick"
